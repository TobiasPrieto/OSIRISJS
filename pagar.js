const totalAPagar = JSON.parse(localStorage.getItem("totalAPagar"));   //*se declara la variable totalAPagar y se le asigna el valor del localStorage
const productoLocalStorage = JSON.parse(localStorage.getItem("canasta"));//*se declara la variable prodocutoLocalStorage y se le asigna el valor del localStorage

